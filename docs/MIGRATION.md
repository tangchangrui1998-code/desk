# FoeDesk 迁移说明

完整迁移计划的当前权威版本位于本机只读参考项目：

```text
../travel-collection/PURE_DESKTOP_PET_MIGRATION.md
```

本项目严格遵循该计划，已完成：

- 阶段 0：建立独立的 Tauri 2 + React + TypeScript 项目和产品标识。
- 阶段 1：迁移透明窗口、置顶、单实例、拖动、缩放、多显示器和窗口位置恢复能力。
- 阶段 2：建立独立人物状态和 v2 版本化存档，接入小野、莓莓、白桃与门后人偶。
- 阶段 3：实现数据化点击区域、互动冷却、关系成长、情绪恢复、昼夜主动对白和 8 个视觉反馈状态。
- 阶段 4：实现 Provider 抽象、系统凭据库中的 DeepSeek Key、每人物独立历史、用户事实、关系事件、摘要和记忆检索；离线模式始终可用。
- 阶段 5：迁移衣柜、45 条批量暗号及赤红/蓝色机甲后台主题；删除旧旅行、掉落、兑换和旅尘条件。
- 阶段 6：迁移 61 张原画为 1024×1536 WebP，并追加 5 套同规格原创皮肤；当前共 66 张运行图与 66 张 200×300 缩略图。

资源映射、旧来源和身份分类审计见 `SKIN_MIGRATION_AUDIT.json`。`mystery-code-moon-tide` 的旧原图没有透明通道，因此明确使用 `backdrop` 渲染模式，其余外观使用透明模式。

新项目不包含旅行、地图、照片、相册、收藏、掉落、旅尘或重复刷取养成。AI 只负责语言表现，不直接写入关系数值或解锁。

旧项目 `../travel-collection/` 为只读参考，不加入 workspace，不从新项目构建，也不应被新代码导入。
